{% extends "base.jinja" %}

{% block content %}
<div class="container">
    <div class="row">
        <div id="breadcrumbs">
            <ol class="breadcrumb">
                <li><a href="{{ url("home") }}">Головна</a></li>
                <li class="active">{{ page.title }}</li>
            </ol>
        </div>
        <section id="profile" class="col-lg-8">
            <h1>{{ page.title }}</h1>
            <div>
                {{ page.body|safe }}
            </div>
        </section>
    </div>
</div>
{% endblock %}