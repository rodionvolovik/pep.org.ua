{% extends "base.jinja" %}

{% block content %}
<div class="container">
    <div class="row">
    
        <div id="breadcrumbs">
            <ol class="breadcrumb">
                <li><a href="{{ url("wagtail_serve", "") }}">{{ _("Головна") }}</a></li>
                <li class="active">{{ _("Напишіть нам листа") }}</li>
            </ol> 
        </div>
        
        <section id="search-by-country">
            <h1>{{ _("Напишіть нам листа") }}</h1>
            <div class="page-content">
                <div id="data-send-form">
                    <div class="form-container">
                        <div class="form-content">
                            <div class="form">
                                {% include "_feedback_form.jinja" %}    
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>

</div>
</div>
{% endblock %}